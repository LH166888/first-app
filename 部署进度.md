# 车榜(che-bang)外网部署进度记录

> 本文档记录项目对外访问的部署方案、决策过程与后续步骤，作为项目记忆文件持续更新。
> 最后更新：2026-09-09（前端已上线；后端开发启动，详见第八节）

## 当前状态：✅ 已上线，国内可正常访问

- 正式访问地址：**https://aihhyy.cn** 、 **https://www.aihhyy.cn**（已验收，国内不挂梯子可访问）
- 托管：Vercel（境外，静态托管，自动 HTTPS）
- 域名：aihhyy.cn（腾讯云，未备案）

## 一、项目概况

- **项目名**：che-bang（车榜 · 汽车销量排行榜与配置查询 Demo）
- **技术栈**：Vue 3 + Vite 5，纯前端静态 SPA（无后端）
- **路由模式**：createWebHashHistory（hash 路由，URL 带 #，刷新子页不会 404，无需服务端重写）
- **构建产物**：npm run build → dist/ 静态文件
- **本地路径**：d:\git\own-lh\first-app（D 盘，注意不是 C 盘）
- **GitHub 仓库**：https://github.com/LH166888/first-app （分支 main）

## 二、目标

让网页在**外网可访问**，**未来长期规划：稳定面向国内用户**。

## 三、方案选型与决策

### 已对比的方案
| 方案 | 国内体验 | 成本 | 结论 |
|------|---------|------|------|
| Vercel（境外静态托管） | 需绑自有域名才能访问，偶尔慢/不稳 | 免费 | 当前采用（起步阶段） |
| 国内 CDN（OSS/COS） | 好 | 低 | 需备案 |
| 国内服务器 + Nginx + 备案 | 最好、最稳 | 服务器约一两百元/年 | 未来长期方案 |
| 内网穿透（ngrok 等） | 临时 | 免费 | 仅临时演示，未采用 |

### 当前决策：先走 A 方案（Vercel + 自有域名，不备案）
- 理由：0 成本、立即可上线，先跑起来验证。
- 已知代价：.cn 域名不备案绑 Vercel，国内能访问但偶尔慢，且 .cn 监管较严有被暂停解析的小风险。
- 未来当项目需要长期稳定面向国内时，再迁移到「备案 + 国内服务器」方案（详见第六节）。

## 四、已完成的工作

1. **优化生产构建**（提交 8c59b14）
   - vite.config.js：分包（vue / chart.js 独立 chunk）、去除 console/debugger、现代浏览器 target（es2018）
   - 首屏产物约 gzip ~120 kB，vue/chart 可长期缓存
2. **新增 vercel.json**
   - 声明 framework=vite、构建命令、输出目录
   - 静态资源长期强缓存（immutable），index.html 不缓存，加速二次访问
3. **代码已推送 GitHub**（origin/main 最新）
4. **Vercel 部署成功**
   - 通过 GitHub 导入 LH166888/first-app 自动部署
   - 临时地址：first-app-mu-opal.vercel.app（国内被墙，不再对外使用）
   - 已验收：功能齐全（排行/详情/对比/收藏）
5. **已购买域名**：aihhyy.cn（腾讯云，注册 2026-09-09，到期 2027-09-09，DNS 托管 DNSPod）
6. **域名绑定成功 ✅**（2026-09-09）
   - 在 Vercel Domains 添加 aihhyy.cn 与 www.aihhyy.cn
   - 在腾讯云 DNSPod 配置解析记录：
     - A 记录：主机记录 @ → 216.198.79.1
     - CNAME 记录：主机记录 www → 7d5f331fc391ef9e.vercel-dns-017.com
   - Vercel 显示「有效配置」，HTTPS 自动签发
   - 国内不挂梯子访问 https://aihhyy.cn 正常

## 五、后续可选事项

- [ ] 是否把 部署进度.md 提交推送到 GitHub（当前仅存本地）
- [ ] 观察国内访问稳定性（Vercel 跨境，偶尔慢属正常）
- [ ] 如需长期稳定/更快，走第六节备案 + 国内服务器方案

## 六、未来长期方案（备案 + 国内服务器）备忘

当需要稳定面向国内时，切换到此方案（与 Vercel 可并存，Vercel 留给国外）：

1. 买国内轻量服务器：腾讯云轻量应用服务器，选国内地域（北京/上海/广州，勿选境外），镜像建议「宝塔面板」，至少买 3 个月（建议 1 年）
2. 提交 ICP 备案（免费，约 3~20 天）：需身份证、人脸核验、域名证书；接入选该服务器；主体选个人
   - 备案通过前，勿将 aihhyy.cn 解析到国内服务器打开未备案站点
   - ICP 备案必须绑定国内服务器，Vercel（境外）无法作为备案接入方
3. 部署：dist/ 上传服务器 → Nginx 托管 → 域名解析到服务器 IP → 一键 HTTPS → 页脚挂备案号

## 七、关键提醒

- 对外统一使用 aihhyy.cn，.vercel.app 临时域名国内打不开（被墙），不再使用
- 代码改动/重新部署：git push 到 main，Vercel 自动重新构建部署，aihhyy.cn 自动更新，无需再动 DNS
- 修改配置文件（vite.config.js / vercel.json 等公共文件）前需先确认
- **本项目真实路径在 D 盘（D:\git\own-lh\first-app）**

## 八、后端开发进度（进行中，2026-09-09 起）

> 项目从「纯前端静态 SPA」扩展为「前端 + 后端 API」。
> 后端技术栈：Laravel（PHP）+ Sanctum 登录认证 + 数据库；代码位于 backend/（当前 git 未跟踪）。

### 当前状态：后端 6 步计划全部完成，9 个接口自测通过 ✅（2026-09-10）

### 计划步骤（按顺序）
1. ✅ **连数据库**：已完成。
   - 环境：phpStudy 自带 MySQL 8.0.12（无 pdo_sqlite，故弃用 Laravel 12 默认的 sqlite，改用 MySQL）
   - `.env`：DB_CONNECTION=mysql，HOST=127.0.0.1，PORT=3306，DATABASE=chebang，USERNAME=root，PASSWORD=root（已备份原文件为 `backend/.env.bak`）
   - 已创建数据库 `chebang`（utf8mb4 / utf8mb4_unicode_ci）
   - **踩坑记录**：该 MySQL 实例全局默认存储引擎是 MyISAM，导致 users 表 email 唯一索引超长报错（MyISAM 索引上限 1000 字节，utf8mb4×255 超限）。**只改了项目内 `backend/config/database.php`**，给 mysql 连接显式指定 `'engine' => 'InnoDB'`，未动 phpStudy 全局配置，不影响机器上其他项目。
   - 已跑通 Laravel 默认迁移（users / cache / jobs 等），`migrate:status` 全部 Ran，表引擎确认均为 InnoDB。
2. ✅ 安装 **Sanctum**（登录认证）：已完成。
   - `composer require laravel/sanctum`（v4.3.3）
   - `php artisan install:api`：生成 `routes/api.php`（含示例 `GET /api/user`，挂 `auth:sanctum` 中间件），自动在 `bootstrap/app.php` 注册 `api:` 路由
   - 迁移新增 `personal_access_tokens` 表（InnoDB，同第 1 步的引擎修复）
   - `app/Models/User.php`（核心文件，已征得确认）加上 `Laravel\Sanctum\HasApiTokens` trait
   - 验证：`migrate:status` 全部 Ran，`route:list --path=api` 可见 `api/user` 路由
3. ✅ **建表迁移**：cars、favorites（users 表 Laravel 自带）：已完成。
   - 数据形状参考前端 `src/data/cars.js`：`id` 为字符串 slug（如 `byd-song-plus`），前端路由/收藏/对比均依赖它，**已与你确认**：cars 表主键采用字符串 slug（而非数字自增 + 映射列）。
   - `cars` 表字段：`id`(string, PK) / `name` / `brand` / `energy`(ev\|phev\|fuel) / `level`(轿车\|SUV\|MPV) / `price_min`,`price_max`(decimal 8,2) / `sales`(unsigned int) / `year`(unsigned smallint) / `trend`(json，12 个月销量) / `config`(json，配置参数，字段因车型而异) / timestamps；对 brand/energy/level 建索引。
   - `favorites` 表字段：`id` / `user_id`(FK→users.id，级联删除) / `car_id`(FK→cars.id，级联删除) / timestamps；`(user_id, car_id)` 联合唯一索引防重复收藏。
   - 验证：两表引擎均为 InnoDB，外键约束、唯一索引均已生效，`migrate:status` 全部 Ran。
4. ✅ 写 **Seeder**：已完成。
   - `database/seeders/CarSeeder.php`：把前端 `src/data/cars.js` 的 16 款车「一比一」硬编码转写为 PHP 数组（不做运行时解析 JS 的取巧方案，更可靠、无额外依赖）。
   - 字段映射：`price[min,max]` 拆为 `price_min`/`price_max`；`trend`/`config` 用 `json_encode(..., JSON_UNESCAPED_UNICODE)` 存为 JSON；`year` 统一取 cars.js 的 `YEAR = 2023`。
   - 用 `DB::table('cars')->upsert(..., ['id'], [...])`，重复执行 Seeder 时按 `id` 更新而非报唯一键冲突。
   - 已执行 `php artisan db:seed --class=CarSeeder` 并校验：**16 条**入库，抽查 byd-song-plus 字段正确，trend 12 个月、config 12 键、中文正常；能源分布 ev=7 / fuel=6 / phev=3。
5. ✅ 建 **Model + Controller + API 路由**：已完成。
   - Model：`Car`（主键 string 非自增；`trend`/`config` casts 为 array；追加 `price:[min,max]` 访问器并隐藏 `price_min/max`，贴合前端 cars.js 形状）、`Favorite`（belongsTo user/car）；`User` 新增 `favorites()` hasMany 关联（纯新增，不改原有行为）。
   - Controller（`app/Http/Controllers/Api/`）：
     - `CarController`：`index`（默认按销量降序返回全部，支持 `?energy/level/brand/keyword` 筛选）、`show`（详情，404 处理）。
     - `AuthController`：`register`（校验+建用户+返回 token）、`login`（校验邮箱密码+返回 token）、`logout`（删当前 token）。
     - `FavoriteController`：`index`（我的收藏，返回车型完整信息+id 列表）、`store`（`firstOrCreate` 防重复）、`destroy`（取消收藏）。
   - 路由（`routes/api.php`，共 9 条，`route:list` 已核对）：
     - 公开：`GET /api/cars`、`GET /api/cars/{id}`、`POST /api/register`、`POST /api/login`
     - 需登录（`auth:sanctum`）：`GET /api/user`、`POST /api/logout`、`GET /api/favorites`、`POST /api/favorites`、`DELETE /api/favorites/{carId}`
6. ✅ **php artisan serve** 测试接口：已完成。
   - 后台 `php artisan serve --host=127.0.0.1 --port=8000` 启动，`route:list --path=api` 见 9 条路由。
   - 公开接口自测：`GET /api/cars` 按销量降序（秦PLUS 48万居首）；`?energy=ev`→7 条、`?level=MPV`→2 条、`?keyword=宋`→命中 byd-song-plus；`price` 正确输出为 `[min,max]` 数组；详情不存在→404；未登录访问收藏→401。
   - 认证+收藏闭环自测（Sanctum token）：注册→返回 user+token；带 token `GET /user` 正常；收藏→201、重复收藏幂等→仍201（`firstOrCreate`）、收藏列表 `ids` 正确；收藏不存在车型→422（`exists` 校验）；取消收藏→200 且列表更新；错误密码登录→422；登出→200，登出后旧 token 再访问→401。
   - 测试用户已清理：最终 users=0 / cars=16 / favorites=0。
   - **踩坑记录（重要）**：本机 C 盘存在指向 D 盘项目的路径别名，对「新建文件」用 C 盘绝对路径 Write 会写空/写偏，但 serve/Read 读的是 D 盘真实文件 → 表现为「保存成功但方法丢失、接口报 undefined method」。**规避：一律用 D 盘真实绝对路径（或相对 cwd 路径）读写；已存在文件改用 Edit 更稳。**
   - **另一坑（非代码问题）**：Windows git bash 用 curl.exe 传含中文的 JSON 会编码损坏，导致后端收到空 body 报「字段必填」。仅为命令行测试假象，前端发 UTF-8 JSON 不受影响。

### 待确认 / 提醒
- 数据库名：chebang（已生效）。
- `.env`、`config/database.php` 均属公共配置文件，本次改动前已征得确认；后续再改仍会先问。
- 如迁移或数据丢失需重来，`backend/.env.bak` 是切库前的原始 `.env`（sqlite 配置）。

## 九、前端接入 API（阶段 A 本地联调，2026-09-10 完成 ✅）

> 让前端不再读写死数据 / 假登录 / localStorage 收藏，改调真实后端。
> 本地 `vite dev :5180` 经 vite proxy 连本地 Laravel `:8000`，读 / 认证 / 收藏三链路已跑通（用户手测通过）。

### 已完成
- **基础设施**：装 axios；`vite.config.js` 加 proxy（`/api` → `127.0.0.1:8000`）；新增 `.env.development`（`VITE_API_BASE_URL=/api`）、`.env.production`（占位）、`.env.example`。
- **API 层** `src/api/`：`client.js`（axios 实例 + token 请求拦截器，401 响应统一清理登录态）、`cars.js`、`auth.js`、`favorites.js`。
- **store 改造** `src/store.js`：token 持久化（key `chebang_token`）、`carsById` 缓存；`login/register/logout` 改 async 走后端；`toggleFavorite` 乐观更新 + 失败回滚；登录后拉 `listFavorites`。
- **视图/组件**：RankingView / CarDetailView 改 onMounted/watch 拉接口 + 加载/空态；Compare / Favorites 改用 store 缓存按 id 取车；新增 `AuthModal.vue`（真登录/注册表单，422 字段级错误展示）接入 App.vue。
- **清理**：纯展示常量抽到 `src/data/constants.js`，删除写死的 `src/data/cars.js`；`vite build` 通过。

### 未做（后续步骤）
- **git**：`backend/` 未跟踪、前端改动未提交。⚠️ 新前端依赖后端，直接 push 到 main 会触发 Vercel 重建，而线上无后端 → aihhyy.cn 会挂；须待后端上线后再切前端（建议先在分支保存成果）。
- **目录重构 monorepo**（架构第五节）、**后端部署**（阶段 B/C）——均未开始。
