# 后端部署计划（Render + Docker + Aiven MySQL）

> 本文档是「后端部署」这一阶段的操作手册与决策记录，按步骤执行、执行完打勾。
> 创建：2026-09-10　最后更新：2026-09-10

## 〇、背景与目标

前端（aihhyy.cn）已上线 Vercel，后端（Laravel 12 + Sanctum）本地联调通过，现需**部署到公网**，让线上前端能调通。

- **目标**：后端与数据库上公网，前端切生产指向后可登录/收藏。
- **优先级**：国内访问体验 > 免费 > 省事。**已接受**免费方案下跨境延迟（每次请求多零点几秒）。
- **明确不做**：ICP 备案 / 国内服务器（长期方案，另见 `部署进度.md` 第六节）。

## 一、最终选型（已决策）

| 项 | 选择 | 理由 |
|----|------|------|
| 后端托管 | **Render**（Docker 部署） | 免费额度可用；Docker 对 PHP 扩展 / SSL 证书最可控 |
| 数据库 | **Aiven MySQL**（免费永久档） | 免费且数据持久；**必须选 Singapore 区域**与后端同区 |
| 连接方式 | Laravel 直连 MySQL，强制 SSL | Aiven 要求 SSL |
| 实例类型 | Render **Free**（先跑通） | 0 成本；代价=15 分钟空闲休眠（30-60s 冷启动）|

### 为什么不用其它
- **SQLite**：Render 免费版磁盘临时，重部署丢用户数据（已排除）。
- **Render 付费磁盘**（$7.25/月）：可永久持久且免冷启动，作为**付费备选**保留。
- **CloudBase**：文档型 DB，与 MySQL 不兼容，需另买 MySQL。

### 已知代价（必须接受）
1. **冷启动**：免费实例 15 分钟无请求即休眠，下次访问 30-60s 唤醒。
   - 缓解：外部定时器（cron-job.org / UptimeRobot）每 10 分钟打 `GET /up`（[bootstrap/app.php](../../d:/git/own-lh/first-app/backend/bootstrap/app.php) 已内置 `/up` 健康路由）。
   - 彻底解决：升级 Render Starter（$7/月，不休眠）——本阶段不做。
2. **跨境延迟**：国内访问每次请求多约 0.1-0.25s（后端新加坡）。已评估为可接受。
3. **Aiven 闲置可能被关机**：Aiven 对长期无活动实例会自动关机（有邮件、可自行唤醒）。后端有真实请求 + 保活，正常不受影响。

## 二、前置准备（用户本人操作）

- [ ] **1. 注册 Aiven**（https://aiven.io ，无需信用卡）
  - 建 **MySQL** 实例，**Region 选 Singapore（亚太-新加坡）**，Plan 选 **Free**
  - 建库后拿到：`Host` / `Port`（**非 3306**）/ `User` / `Password` / `Database`（默认 `defaultdb`）
  - 下载 **CA 证书**（Aiven 控制台 → 该服务 → CA Certificate → 下载 `ca.pem`），后面要放进项目
- [ ] **2. 注册 Render**（https://render.com ，用 GitHub 登录）
- [ ] **3. 确认已推送**：后端代码（含本阶段新增文件）已推到 GitHub 仓库 `LH166888/first-app`

## 三、代码改动清单（本阶段产出）

> 公共文件 / 影响面大的改动已在下表标注 ⚠️，执行前单独确认。

| # | 文件 | 内容 | 影响面 |
|---|------|------|--------|
| 1 | `backend/Dockerfile` | PHP 8.2-cli + `pdo_mysql` / `mbstring` / `bcmath` 等扩展；Apache 或 `php artisan serve` 起服务 | 新增 |
| 2 | `backend/docker/apache.conf`（若用 Apache） | DocumentRoot 指向 `public/`，重写规则 | 新增 |
| 3 | `backend/docker/start.sh` | 容器启动脚本：`migrate --force` → `db:seed --force` → `config:cache` → 起服务 | 新增 |
| 4 | `backend/.dockerignore` | 排除 `vendor/`、`.env`、`node_modules` 等，减小构建体积 | 新增 |
| 5 | ⚠️ `backend/database/seeders/DatabaseSeeder.php` | **删掉** `User::factory()->create(['email'=>'test@example.com'])`，改为只 `$this->call(CarSeeder::class)` | 公共 seeder |
| 6 | `backend/.env.production.example` | Render 环境变量模板（MySQL + SSL + 生产配置） | 新增 |
| 7 | `backend/config/database.php` | ⚠️ 给 mysql 连接**新增** `MYSQL_ATTR_SSL_CA` 等选项（走 env 开关，默认不影响本地） | ⚠️ 公共配置 |
| 8 | `部署/Render部署.md`（或并入本文档） | Render 逐步操作手册 | 新增 |

## 四、执行步骤

### 阶段 1 · 代码准备（我执行，你 review）
1. 按第三节清单逐个产出文件
2. 本地验证：
   - `docker build` 能构建成功
   - 本地用 docker 跑一遍，`/up` 返回 200、`/api/cars` 返回 16 条
   - 确认 `DatabaseSeeder` 不再插测试用户
3. 提交到分支（**不推 main**，避免触发 Vercel 重部署）

### 阶段 2 · 建库建服务（你操作，我给指引）
4. Aiven 建 MySQL（第二节步骤 1），拿到连接信息
5. Render 建 Web Service：
   - 连接 GitHub 仓库、分支
   - **Root Directory 填 `backend`**
   - Environment 选 **Docker**
   - 填入环境变量（见第五节）
   - 挂载 CA 证书：用 Render 的 **Secret File** 功能，把 `ca.pem` 内容存为 `/etc/ssl/certs/aiven-ca.pem`

### 阶段 3 · 验证
6. 部署完成后访问 `https://<服务名>.onrender.com/up` → 应返回 200
7. 测 `GET /api/cars` → 16 条；`POST /api/login` 冒烟
8. 检查 Render 日志：迁移是否成功、seed 是否成功

### 阶段 4 · 保活 + 前端切换（后端跑通后另开）
9. 配 cron-job.org 每 10 分钟打 `/up`
10. 前端 `.env.production` 填 `VITE_API_BASE_URL=https://<服务名>.onrender.com/api`
11. 后端 CORS 放行 `aihhyy.cn` / `www.aihhyy.cn`
12. 联调通过后再 merge 到 main

## 五、环境变量清单（Render Environment）

```
APP_NAME=chebang
APP_ENV=production
APP_KEY=<本地 php artisan key:generate --show 生成，与本地不同>
APP_DEBUG=false
APP_URL=https://<服务名>.onrender.com
APP_TIMEZONE=Asia/Shanghai

DB_CONNECTION=mysql
DB_HOST=<Aiven Host>
DB_PORT=<Aiven Port>
DB_DATABASE=defaultdb
DB_USERNAME=<Aiven User>
DB_PASSWORD=<Aiven Password>
DB_SSL_CA=/etc/ssl/certs/aiven-ca.pem

SESSION_DRIVER=cookie
CACHE_STORE=file
QUEUE_CONNECTION=sync
LOG_CHANNEL=stderr
LOG_LEVEL=warning
```

> `LOG_CHANNEL=stderr` 让日志直接进 Render 日志面板；不要写文件（容器文件系统临时）。

## 六、风险与注意

- **Aiven 端口非 3306**，别按习惯填 3306。
- **Aiven 强制 SSL**，`config/database.php` 必须带上 CA 路径，否则连接被拒。
- Render **Free 实例磁盘临时**，但因数据在 Aiven，容器本身无状态，重建不影响数据。
- **首次部署的 `migrate --force` 会建表**；`db:seed --force` 幂等（`upsert`），重复部署安全。
- **不要 push main**：前端还没切指向，线上无后端，push main 会让 aihhyy.cn 报错。
- 后端代码目前 `backend/` 未跟踪 / 在 `feat/api-integration` 分支——本阶段继续在该分支工作。

## 七、进度记录

| 阶段 | 状态 | 完成时间 |
|------|------|---------|
| 阶段 1 代码准备 | ✅ 本地 docker 验证通过（/up 200、/api/cars 16 条、migrate+seed 成功） | 2026-09-11 |
| 阶段 2 Aiven + Render 建服务 | ✅ Aiven MySQL(新加坡) + Render Docker 服务已上线 | 2026-09-14 |
| 阶段 3 部署验证 | ✅ /up 200、/api/cars 16 条、/api/login 422(验证正常) | 2026-09-14 |
| 阶段 4 保活 + 前端切换 | ✅ cron-job.org 保活已配；前端 .env.production 指向 Render；已 merge 到 main（Vercel 生产重建，www.aihhyy.cn 切真后端） | 2026-09-14 |

> 阶段 1 产出：`backend/Dockerfile`、`backend/docker/{apache.conf,start.sh}`、`backend/.dockerignore`、
> `backend/.env.production.example`、改后的 `DatabaseSeeder.php`、`部署/Render部署.md`。
> 修正记录：① `.dockerignore` 勿排除 `docker/`；② `start.sh` 去掉 `route:cache`（闭包路由会崩）；
> ③ SSL 变量名应为 `MYSQL_ATTR_SSL_CA`（非计划里的 `DB_SSL_CA`；第五节环境变量清单里的 `DB_SSL_CA` 一行应据此改为 `MYSQL_ATTR_SSL_CA`）。
>
> 本地 docker 验证阶段又修正（均为「不修上线必炸」的真实 bug）：
> ④ 补齐 `backend/.env.production.example`（第三节清单第 6 项，之前缺失）。
> ⑤ `.dockerignore` 增排除 `/bootstrap/cache/*.php`——否则本地(含 dev 依赖)生成的包发现缓存被 `COPY . .` 打进镜像，
>    `--no-dev` 生产镜像启动时引用未安装的 dev 包 `laravel/pail` → 启动即崩；改后运行时由 Laravel 按线上依赖自动重建。
> ⑥ Dockerfile `a2enmod rewrite` → `a2enmod rewrite headers`——`apache.conf` 用了 `Header` 指令给静态资源设缓存头，
>    需 `mod_headers` 模块，否则 Apache 报 `Invalid command 'Header'` 拒绝启动。
> ⑦ 验证方式：本地起临时 MySQL 容器 + 后端容器（同 docker 网络），空库跑通 migrate/seed，幂等重跑亦正常。
>
> 上线到 Render 后又修正（跨境真实部署才暴露）：
> ⑧ Render 的 Secret File 用**纯文件名**上传（`aiven-ca.pem`），挂载点是 `/etc/secrets/aiven-ca.pem`（第二节/第五节里 `/etc/ssl/certs/...` 的写法不成立，Secret File 文件名不能带斜杠）。
> ⑨ **CA 证书权限坑（曾致 /api/cars 500）**：`/etc/secrets/aiven-ca.pem` 属主 root、权限收紧；`start.sh` 里的 `migrate/seed` 以 root 跑能读到（故迁移成功、`/up` 200），但 Apache 请求以 www-data 身份跑读不到 → `PDOException: failed loading cafile stream`。
>    修法：`start.sh` 在 `config:cache` 前把证书 `cp` 到 `storage/certs/aiven-ca.pem` 并 `chmod 644`，环境变量 `MYSQL_ATTR_SSL_CA` 指向 `/var/www/html/storage/certs/aiven-ca.pem`（不是 `/etc/secrets/...`）。
