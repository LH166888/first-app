# 车榜(che-bang)前后端分离 · 架构设计

> 本文档为车榜项目从「纯前端」升级到「前后端分离」的架构设计与落地方案。
> 创建于 2026-09-09，持续更新。

## 一、背景与目标

- **现状**：纯前端 Vue 3 + Vite，车型数据写死在 src/data/cars.js，登录为假登录，收藏存 localStorage。
- **目标**：改造为前后端分离——后端提供 API + 数据库，支持真实注册/登录、收藏落库、车型数据可管理。
- **约束**：最大程度优化国内体验；成本上「免费优先」。

## 二、改造要点(现状 → 改造后)

| 现状 | 改造后 |
|------|--------|
| cars.js 静态数据 | MySQL 存车型，后端 API 提供 |
| 假登录（仅本地） | 真实注册/登录（Laravel Sanctum，token 认证） |
| 收藏存 localStorage | 收藏存数据库，跟用户走、多端同步 |
| 对比 compareIds | 保持前端本地（临时状态，无需后端） |

## 三、总体架构

> 下图为**实际上线拓扑**（2026-09-14 公网部署完成）；最初设想的「前后端同主域 + `api.` 子域名」未采用，改为前端用自有域名、后端直接用 Render 的 onrender.com 域名，差异见节末。

```
                用户浏览器
                    | HTTPS
       +------------+--------------+
       |                           |
 www.aihhyy.cn         chebang-backend.onrender.com
 (前端 Vue SPA)              (后端 Laravel API)
 Vercel(全球 CDN)               | Docker 容器(新加坡)
                               | 强制 SSL
                        Aiven MySQL(新加坡)
```

- 前端：Vue SPA，Vercel 托管；`.env.production` 的 `VITE_API_BASE_URL` 指向后端 API。
- 后端：Laravel 12 只提供 JSON API，Docker 化部署于 Render（新加坡区），容器无状态。
- 数据库：Aiven MySQL（免费永久档，新加坡区），强制 SSL；与后端同区，降低跨网延迟。
- 跨域与认证：后端 CORS 放行前端来源；认证走 Bearer token（放 localStorage，无 cookie，故无需前后端同源）。

> **与设计稿差异**：① 未启用 `api.aihhyy.cn` 子域名（省去 DNS/证书配置），后端直接用 `chebang-backend.onrender.com`；② 未上对象存储（车型图暂不涉及）。后续若要国内加速或统一域名，再给 Render 挂自定义域名即可。

## 四、技术栈选型

| 层 | 选型 | 说明 |
|----|------|------|
| 前端 | Vue 3 + Vite（现有） | 部署 Vercel（免费），域名 www.aihhyy.cn |
| 后端 | Laravel 12（PHP 8.2+） | 只做 API（实际用 Laravel 12，非最初设计的 11）；生产 Docker 镜像部署 Render（新加坡，Free 档）|
| 认证 | Laravel Sanctum | SPA/API token 认证（Bearer token） |
| 数据库 | MySQL 8 | 开发本地 phpStudy MySQL；生产 Aiven MySQL（新加坡，强制 SSL） |
| 后台管理(可选) | Filament | 快速做车型管理后台（暂未做） |

> 生产部署的完整操作手册、环境变量、踩坑修正记录见 `后端部署计划.md`。

> Laravel 适配性说明：车榜以「读」为主，Laravel 略重但完全胜任，适合作为学习项目；
> 注意 PHP 的「免费 + 国内」托管比 Node 难，故部署分阶段推进（见第八节）。

## 五、目录结构（Monorepo 单仓库）

```
chebang/
├── frontend/                   # 现有 Vue 迁入
│   └── src/
│       ├── api/                # 新增：封装 axios 调后端
│       │   ├── client.js       # axios 实例(baseURL、token 拦截器)
│       │   ├── cars.js         # 车型接口
│       │   └── auth.js         # 登录/注册接口
│       ├── store.js            # 改：收藏走 API
│       └── views/ ...
│
└── backend/                    # Laravel
    ├── app/
    │   ├── Models/             # Car, User, Favorite
    │   └── Http/Controllers/Api/
    │       ├── CarController.php
    │       ├── AuthController.php
    │       └── FavoriteController.php
    ├── routes/api.php          # API 路由
    ├── database/
    │   ├── migrations/         # 建表
    │   └── seeders/            # 把 cars.js 数据灌进库
    └── .env                    # 数据库密码等(不进 git)
```

> 落地决策（2026-09-14 已定）：**重构现有 first-app 仓库**（未新建 chebang 仓库），
> 前端从根目录 `git mv` 迁入 `frontend/`，与 `backend/` 对称；Vercel 项目 Root Directory 已改为 `frontend`。
> 迁移用 `git mv` 保留历史，构建产物哈希前后一致（功能等价），已合并进 main、生产站从新结构重建。

## 六、数据库表设计

### cars（车型）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string(PK) | 如 byd-song-plus |
| name / brand | string | 名称/品牌 |
| energy | string | ev/phev/fuel（迁移实际用 string 存，未用 enum 类型） |
| level | string | 轿车/SUV/MPV |
| price_min / price_max | decimal | 价格区间(万) |
| sales | int | 年度销量 |
| year | int | 年份 |
| trend | json | 12 个月走势数组 |
| config | json | 配置对象(直接存 JSON) |

### users
- id、name、email、password、timestamps（Laravel 标准）

### favorites（收藏，多对多）
| 字段 | 说明 |
|------|------|
| user_id | 外键 → users |
| car_id | 外键 → cars |
| (user_id, car_id) 联合唯一 | 防重复收藏 |

> trend、config 用 JSON 字段存储，贴合现有结构，无需拆多张小表。

## 七、API 接口设计（REST）

> 下表为**实际落地**的接口（以 `backend/routes/api.php` 为准，2026-09-10 联调通过），与最初设计稿有出入，差异见表后说明。

| 方法 | 路径 | 作用 | 需登录 |
|------|------|------|--------|
| GET | /api/cars | 车型列表，默认按 sales 降序；可选筛选 `?energy=&level=&brand=&keyword=`；返回 `{data,total}` | 否 |
| GET | /api/cars/{id} | 车型详情(含 trend/config)；返回 `{data}`，不存在 → 404 `{message}` | 否 |
| POST | /api/register | 注册，body `{name,email,password}`；返回 `{user,token}`，201 | 否 |
| POST | /api/login | 登录，body `{email,password}`；返回 `{user,token}`；校验失败 422 | 否 |
| GET | /api/user | 当前登录用户信息 | 是 |
| POST | /api/logout | 退出，删除当前 token；返回 `{message}` | 是 |
| GET | /api/favorites | 我的收藏；返回 `{data:[car], ids:[car_id]}` | 是 |
| POST | /api/favorites | 加收藏，body `{car_id}`；`firstOrCreate` 幂等，201 | 是 |
| DELETE | /api/favorites/{carId} | 取消收藏；返回 `{message}`，200 | 是 |

> **与最初设计稿的差异（已回填）：**
> 1. **无独立 `GET /api/rankings` 接口**：销量排行改由 `GET /api/cars` 默认按 sales 降序实现，前端 TOP 榜直接复用列表结果，避免重复接口。
> 2. **`/api/cars` 筛选维度**：设计写 `energy/level/year`，实际为 `energy/level/brand/keyword`（去掉按年筛选，新增品牌与关键词，对齐前端筛选器）。
> 3. **加收藏改为 `POST /api/favorites` + body `{car_id}`**（原设计 `POST /api/favorites/{carId}`）：与「取消收藏」用路径参数区分，body 传参更便于 `exists:cars,id` 校验。
> 4. **新增 `GET /api/user`**：Sanctum 默认的当前用户接口，登录态校验用得上（设计稿第七节漏列）。
> 5. **收藏列表返回 `{data,ids}` 双字段**：`data` 是完整车型（收藏页直接渲染），`ids` 是纯 id 列表（前端同步 `isFavorite` 状态）。

## 八、部署与国内优化方案（分阶段）

前提认知：前端(静态)天然适合免费托管；后端(常驻服务)免费必有代价(休眠/额度小/境外)。
后端与数据库须「同平台/同区域」部署，避免跨网延迟。

### 阶段 A：纯本地开发（当前采用）🆓
- Laravel + 本地 MySQL / SQLite（小皮 phpstudy 提供 PHP+MySQL）
- 前端 vite dev 连本地后端
- 0 成本、0 网络问题，先把功能全部做通。

### 阶段 B：全免费上线（境外）✅ 当前线上采用（2026-09-14）
- 后端：**Render**（Docker 部署，新加坡 Free 档）。代价=15 分钟空闲休眠，用 **cron-job.org 每 10 分钟打 `/up`** 保活。
- 数据库：**Aiven MySQL**（免费永久档，新加坡，强制 SSL），与后端同区。
  - 选型说明：最初设想 TiDB Serverless / Railway，实际选 Aiven——数据持久、免费永久、与 Render 同区（新加坡）延迟最低。
- 前端：Vercel，`.env.production` 指向 Render 后端；merge 到 main 后生产站自动切换。
- 代价：国内访问跨境延迟每请求约 0.1-0.25s（已评估可接受）。
- 详细操作手册与踩坑记录见 `后端部署计划.md`。

### 阶段 C：免费 + 偏国内（后续目标）🟢
- 后端 + 数据库：腾讯云 CloudBase 云托管 + CloudBase 数据库（国内节点 + 免费额度）
- 配置略折腾，额度有限，但「不花钱又要国内快」的最优解。

### 阶段 D：正经国内上线（长期）
- 便宜轻量服务器自建 Laravel + MySQL + api.aihhyy.cn 解析 + 备案
- 少量花钱，最稳最快。

> 迁移成本低：切换阶段主要改数据库连接与前端 API baseURL，业务代码基本不动。

## 九、当前决策与进度

- [x] 确定技术栈：Vue(现有) + Laravel 12 + MySQL + Sanctum
- [x] 确定部署路线：先阶段 A(本地)，后续走阶段 C(CloudBase 偏国内)
- [x] 本地环境确认：phpStudy PHP 8.2.9（≥ 8.2）、Composer 已装
- [x] 决定仓库形态：**重构现 first-app 仓库**为 `frontend/` + `backend/` 对称结构（2026-09-14 完成，见第五节）
- [x] 搭建 Laravel 后端骨架、建表、灌入 cars 数据、写 /api/cars（9 接口自测通过）
- [x] 前端抽出 api/ 层，替换写死数据为接口调用（已删 src/data/cars.js）
- [x] 接入真实登录与收藏落库（阶段 A 本地联调已跑通：读 / 认证 / 收藏三链路）
- [x] **阶段 B 公网上线（2026-09-14）**：后端 Docker 化部署 Render(新加坡) + Aiven MySQL(新加坡, SSL)，前端 Vercel 切真后端并 merge 到 main，cron-job.org 保活
- [x] 生产验证：`/up` 200、`/api/cars` 16 条、`/api/login` 校验正常、www.aihhyy.cn 端到端通

## 十、备注

- 本项目真实路径在 D 盘（C:\Users\gj\first-app）；本环境有路径别名，操作项目文件请用相对路径。
- 域名现状：前端 www.aihhyy.cn（Vercel）；后端暂用 Render 的 chebang-backend.onrender.com，**未启用** `api.aihhyy.cn` 子域名（省去 DNS/证书配置，需要时再挂自定义域名）。
