# 怀旧捕鱼游戏 — 调参指南与影响分析

> 版本 1.0 | 适用于数值策划与服务端开发

## 一、调参基本原则

1. **每次只调一类参数**（赔率 OR 捕获率 OR 权重），变更后必跑 RTP 验证
2. **小步迭代**：单参数调整幅度 ≤20%，观察 RTP 变化
3. **真机埋点优先**：模拟器只保证 RTP 收敛，体验需真人玩测

---

## 二、核心参数调整影响表

| 参数 | 调整方向 | 对 RTP 影响 | 对体验影响 | 风险 |
|------|---------|-----------|-----------|------|
| **小鱼赔率** ↑ | 提高 | RTP ↑ | 节奏变快，金币波动小 | RTP 超标 → 经济失衡 |
| **大鱼赔率** ↑ | 提高 | RTP ↑ | 爆发感 ↑，追大鱼动力强 | 方差过大 → 挫败感强 |
| **小鱼捕获率** ↑ | 提高 | RTP ↑ | 新手友好，正反馈频繁 | 过高 → 失去挑战 |
| **大鱼捕获率** ↑ | 提高 | RTP ↑ | 大鱼命中率 ↑ | 稀有性丧失，期待降低 |
| **小鱼权重** ↑ | 提高 | RTP 轻微 ↑ | 画面单调，缺惊喜 | 无大风险 |
| **大鱼权重** ↑ | 提高 | RTP 取决于大鱼 RTP | 惊喜度 ↑ | 大鱼 RTP<1 时反降总 RTP |
| **特殊鱼权重** ↑ | 提高 | 视特效倍数 | 金币雨/连锁出现频繁 | 洗劫经济（需配合捕获率下调）|
| **每日免费金币** ↑ | 提高 | 无直接影响 | 单局时长 ↑ | 金币贬值，付费转化 ↓ |
| **射速** ↑ | 提高 | 无影响 | 爽快度 ↑，单局变短 | 金币消耗加速 |
| **炮倍加成系数** ↑ | 提高 k 值 | 高倍炮 RTP ↑ | 高倍炮使用率 ↑ | k>0.01 会让 10×炮 RTP>1 |

---

## 三、典型调参场景

### A. 玩家反馈「金币掉太快」

**症状**：平均单局时长 <6 分钟，差评率高

**诊断**：
1. 先查 RTP — 若 <0.92，说明捕获率偏低
2. 再查炮倍分布 — 若玩家高频用 5×/10× 打小鱼，消耗加速但收益未跟上

**解决方案（二选一）**：

| 方案 | 操作 | 副作用 |
|------|------|--------|
| **提高小鱼捕获率** | baseCapture × 1.1 | RTP ↑，需同步下调大鱼捕获率保持总 RTP |
| **增加每日金币** | 3500 → 4000 | 延长游戏时长，但金币价值感下降 |

**验证**：调整后跑 20 万发蒙特卡洛，确保 RTP 仍在 0.92~0.96

---

### B. 玩家反馈「大鱼太难打，没动力」

**症状**：高倍炮使用率 <10%，玩家只用 1×炮刷小鱼

**诊断**：
1. 大鱼捕获率过低（金龙 1.4%）→ 期望命中数 = 1/0.014 ≈ 71 发
2. 大鱼赔率虽高(60×)，但实际 EV 不足以覆盖「打空」的挫败成本

**解决方案（二选一）**：

| 方案 | 操作 | 说明 |
|------|------|------|
| **提高大鱼视觉出现频率** | 权重 3.5% → 5% | 不改捕获率，只让大鱼多出现（玩家更有机会尝试）|
| **提高中档鱼性价比** | 鲨鱼捕获率 3.4% → 4.5% | 让「可打得动的大鱼」收益提升 |
| **增强炮倍加成** | k = 0.004 → 0.006 | 10×炮 RTP 从 0.975 → 0.995，接近打平 |

**警告**：大鱼捕获率不可直接翻倍（会炸 RTP），只能微调 ±20%

---

### C. 内测发现「元宝鱼/灯笼鱼薅羊毛」

**症状**：玩家蹲守特殊鱼，触发金币雨/连锁后单局收益 >10 万

**诊断**：特殊鱼有效赔率 = payout × specialMultiplier，但权重/捕获率未对应下调

**解决方案**：

```json
// 原配置（假设元宝鱼金币雨 ×2 倍有效赔率）
"元宝鱼": { "payout": 60, "baseCapture": 0.014, "eff": 2.0 }

// 修正：捕获率折半以保持 EV 不变
"元宝鱼": { "payout": 60, "baseCapture": 0.007, "eff": 2.0 }
```

**通用公式**：`调整后捕获率 = 原捕获率 / (1 + 超额倍数)`

---

### D. RTP 验证不通过（偏离 0.92~0.96）

| RTP 实测 | 原因 | 修正方向 |
|---------|------|---------|
| >0.96 | 捕获率过高 OR 大鱼赔率过高 | 全局缩放 baseCapture × 0.95 |
| <0.92 | 捕获率过低 OR 大鱼权重过低 | 全局缩放 baseCapture × 1.05 |
| 波动大(±0.05) | 样本量不足 OR 特殊鱼权重过大 | 增加验证轮数 OR 降低特殊鱼权重 |

---

## 四、高级调参技巧

### 4.1 分层 RTP 设计（推荐）

不同鱼种可以有不同目标 RTP，形成「小鱼耐玩 + 大鱼刺激」梯度：

| 鱼种梯度 | 目标单鱼 RTP | 设计意图 |
|---------|-------------|---------|
| 小鱼(赔率 2~4) | 0.97 | 接近打平，支撑日常游玩 |
| 中鱼(赔率 6~10) | 0.94 | 平衡风险收益 |
| 大鱼(赔率 25~60) | 0.88 | 低 EV 但高方差，追梦型 |

**计算公式**：`baseCapture_i = target_RTP_i × 0.94 / payout_i`

**好处**：避免「大鱼性价比过高导致只打大鱼」或「小鱼太亏导致新手劝退」

---

### 4.2 动态调参（服务端实时调控）

在后端维护可热更的配置表（如 Redis），无需发版即可调整：

```javascript
// 示例：根据当日全服 RTP 实测动态微调捕获率
if (dailyRTP > 0.96) {
  globalCaptureMultiplier = 0.98; // 全局下调 2%
} else if (dailyRTP < 0.92) {
  globalCaptureMultiplier = 1.02; // 全局上调 2%
}
```

**警告**：动态调参需严格埋点监控，避免「改崩」后无法回滚

---

## 五、反作弊与异常检测

| 场景 | 检测阈值 | 处理 |
|------|---------|------|
| 单局分数过高 | >500,000 | 标记账号，人工审计 |
| 大鱼命中率异常 | 金龙捕获率 >10% | 疑似修改客户端，封号 |
| 连续触发特殊鱼 | 10 局内 >5 次金币雨 | 概率 <0.001，疑似外挂 |
| 炮倍切换异常 | 0.1 秒内切换 >10 次 | 疑似脚本，限流 |

---

## 六、上线后持续监控指标

| 指标 | 监控方式 | 预警阈值 |
|------|---------|---------|
| 全服日均 RTP | 后端埋点聚合 | 偏离目标 ±3% |
| 炮倍使用分布 | 前端上报 | 10×炮使用率 <5% OR >40% |
| 单局平均时长 | 前端计时 | <6 分钟 OR >15 分钟 |
| 大鱼命中间隔 | 后端埋点 | 中位数 >3 分钟 |
| 金币消耗曲线 | 按秒统计余额 | 出现断崖式下跌（>500/秒）|

---

## 七、FAQ

**Q1: 为什么炮倍加成要用乘法而不是加法？**

A: 加法式 `+3%/级` 对小鱼影响小，但打大鱼时会让 10×炮的捕获率暴涨 27%，EV 失控。乘法式 `×(1+0.004×级差)` 保证所有鱼的 RTP 提升比例一致。

**Q2: hp(血量) 参数在当前模型里有用吗？**

A: 当前简化模型采用「单发概率捕获」，hp 主要作为**表现层参考**（期望命中数 ≈ 1/baseCapture）和**视觉反馈**（高 hp 鱼需要打多发才「看起来」破防）。若改为累积伤害模型，hp 会直接影响经济。

**Q3: 如何快速定位 RTP 异常的根因鱼种？**

A: 运行脚本中的 `perFish` 统计，看「贡献奖池占比」与「出现率×赔率」是否匹配。若某鱼贡献占比远超预期，说明其 EV 过高。

---

## 八、调参检查清单（每次变更前必查）

- [ ] 修改了哪些参数？记录原值与新值
- [ ] 运行 `node fishing-rtp-simulator.js` 验证 RTP 是否通过
- [ ] 运行 5 轮稳定性测试（200k 发/轮）确认波动 <±3%
- [ ] 检查分炮倍 ROI 是否非递减（`verifyByCannon`）
- [ ] 更新 `games-fishing-config.json` 中的 `lastVerified` 时间戳
- [ ] 若改动影响特殊鱼，手动测试金币雨/连锁是否触发正常
- [ ] 提交前在配置文件注释中说明调参原因与预期影响

---

**建议工作流**：需求 → 草案调参 → 模拟器验证 → 真机小范围测试(100 人) → 埋点观察 24h → 全量发布
