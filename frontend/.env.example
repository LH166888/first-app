# 前端环境变量示例（复制为 .env.development / .env.production 使用）
# 后端 API 基础地址：
#   开发期用 /api（配合 vite.config.js 的 server.proxy 代理到本地后端）
#   生产期填后端真实域名，如 https://api.aihyy.cn/api
VITE_API_BASE_URL=/api

# 菜单模块图片：Cloudflare R2 桶的公开访问域名（形如 https://pub-xxxx.r2.dev）。
# 前端只存 image_key，展示时用它拼完整图片 URL。这是公开域名，非密钥，可入库。
VITE_R2_PUBLIC_DOMAIN=
